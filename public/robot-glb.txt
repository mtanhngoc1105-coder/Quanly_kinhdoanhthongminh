Place your robot GLB file here:

- Filename: robot.glb
- Path: public/robot.glb

You can replace this placeholder by copying your .glb file into the project's `public` folder with the exact name `robot.glb`.

If you don't have a GLB yet, you can test with an external GLB by setting the `src` attribute in the component to an absolute URL (e.g. https://modelviewer.dev/shared-assets/models/Astronaut.glb).

Note: model-viewer is included in `public/index.html` as a quick way to render GLB files in the browser.
